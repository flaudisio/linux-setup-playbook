---
- name: Configure host
  hosts: all

  vars_files:
    - "{{ playbook_dir }}/../config.default.yml"
    - "{{ playbook_dir }}/../config.yml"

  roles:
    - role: packages
      tags: packages
    - role: git
      tags: git
    - role: mise
      tags: mise
    - role: chrome
      tags: chrome
    - role: geerlingguy.docker
      tags: docker
    - role: vscode
      tags: vscode
